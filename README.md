# Documentation Build Project

This folder contains the MkDocs site configuration and content used to build the workshop documentation.

- Build toolchain: MkDocs + Material theme
- Entry config: `mkdocs.yml`
- Content: files under `docs/`

This README exists to satisfy the packaging metadata declared in `pyproject.toml`.
